# 開発記録

## 2026-09-20 — 記録用リポジトリの初期化

- 記録先: https://github.com/issa311sas-hub/shibata
- ユーザー提供の開発・学習計画書を原文のまま保存。
- README、AI作業ルール、実験記録テンプレート、Git除外設定を追加。
- 変更範囲はプロジェクト記録の初期整備。学習コードやデータへの変更なし。
- 検証: 計画書のコピー元と保存先のSHA-256一致、git diff --check。
- データ取得・特徴量作成・学習・評価は未実施。Phase 0は未完了。

## 初期化時点の予定

1. データ提供元・利用条件・収録範囲・取得可能な時刻を確認する。
2. Python環境と依存関係の管理方法を決め、再現手順を整備する。
3. Phase 0のデータ処理操作とGitでの記録を確認する。
4. Phase 0の完了判定後、市場ベースラインへ進む。

## 初期化時点のPhase 0 判定

| 項目 | 状態 |
|---|---|
| DATA | 未確認 |
| LEAKAGE | 未確認（データ未取得） |
| CODE | 未着手 |
| TEST | 未実施 |
| VALIDATION | 未実施 |
| DOCUMENTATION | 初期整備済み・Phase完了時に再確認 |

## 2026-09-21 — Phase 0の環境と操作演習

- 変更対象: pyproject.toml、requirements-lock.txt、src/shibata/phase0.py、tests/test_phase0.py、説明文書、ディレクトリ構成。
- 変更理由: 計画書のPhase 0に従い、再現可能な環境と基本的なデータ処理操作を実行できるようにする。
- 変更内容: Python 3.12仮想環境、固定版NumPy・pandas・PyArrow・pytest、合成データのCSV／Parquet読書き、集計・groupby・merge・sort・shift・rolling・SQLite比較。
- 影響範囲: 新規の環境・演習のみ。本番特徴量、評価指標、学習期間はまだ定義していない。
- リーク確認: 演習で当該行をshiftにより除外し、現在・未来の結果改変に対する履歴不変性をテスト。実データの公表時刻・訂正履歴は未検証。
- テスト方法: pip check、pytest -q、python -m shibata.phase0。
- 結果: 依存関係整合性PASS、4 tests passed。CSV／Parquetの完全往復とSQL集計一致PASS。出力の実データフラグはfalse、モデル評価はNOT_RUN。
- 実行環境: Windows 64bit、Python 3.12.14、numpy 2.2.6、pandas 2.2.3、pyarrow 20.0.0。間接依存はrequirements-lock.txt。
- 調査: JRA-VAN公式SDK・FAQ・規約を確認。出典と未確認事項はdata-sources.mdに記録。
- 実データの学習実験ではないためEXP番号・予測性能は付与しない。合成出力はoutputs/phase0へ保存しGit追跡対象外。
- 環境上の問題: 通常のpython/py/uvコマンドが見つからなかったため、利用可能なPython 3.12から専用venvを作成。共有環境にPyArrowがなかったため専用venvへ固定版を導入して解消した。

### 現在のPhase 0判定

| 項目 | 状態 |
|---|---|
| DATA | 合成データPASS、実データ未確認 |
| LEAKAGE | 演習の時系列テストPASS、実データ未確認 |
| CODE | 演習の実行PASS、依存版と再実行手順を保存 |
| TEST | 4件PASS |
| VALIDATION | 未実施（未知期間の実データなし） |
| DOCUMENTATION | 今回の変更・検証・取得元調査を記録済み |

計画書第24章の全PASS条件を満たしていないため、Phase 0全体は未完了とし、Phase 1には進んでいない。
Phase 0は環境演習だが第24章には未知期間評価も要求されているため、対象外扱いへ勝手に変更せず未実施として記録する。

### 次の作業

1. 既存データまたはData Lab.の利用状況をユーザーに確認する（確認依頼済み）。
2. 取得元・条件・取得時刻の証拠を確認し、少量の実データを読み込む。
3. データ品質と時点整合性を確認し、Phase 0の残る判定項目を整理する。

## 2026-09-21 — 契約・JV-Linkなしのオフライン準備

### 指示と作業範囲

ユーザーから既存データなし・Data Lab.未契約と確認済み。
「契約・インストールがない状態で進められるところまで順に」との指示を受け、環境演習の次に、実データ受入れと市場ベースラインを合成データで予行する準備を実施した。
原計画のPhase完了条件を緩めたものではない。Phase 0/1の実データ判定は未完了、Phase 2以降の学習には未着手。

### 変更と理由

1. 変更対象: ingestionの入力形式・原本保存・時刻選択・O1部分解析・取得要求案、marketモデル、baseline評価、offlineコマンド、demo、テスト、関連文書。
2. 変更理由: 無料体験を始める前に、入力不足・時点不整合・再現性の問題を検出できる状態にする。
3. 変更内容: 文字列ID・欠損・重複・頭数の検証、締切以前の同一オッズスナップショット選択、逆数正規化、確率評価、原本ハッシュと実行環境の記録、エラーの保存、上書き防止。
4. 影響範囲: 新規のオフライン処理。既存のPhase 0演習は維持。直接・間接依存の追加なし。Train/Validation/Test期間・本番予測締切は未設定。
5. リーク確認: 結果は予測保存後に読み込み。締切後・最終オッズを選ばない。後日取得履歴には根拠レビューの明示を要求し、当時のローカル取得とは区別する。実データの時刻証拠は未確認。
6. テスト方法: pytest、合成データのCLI実行、取得要求案CLI、入力・出力ハッシュと評価表の確認。

### 検証結果

- 52 tests passed（Phase 0の4件を含む）。
- 合成データCLI: 3レース・6出走行・18オッズ行から、締切以前の6行を選択してPASS。
- CSVとParquetで同じ評価値を確認。別出力先での再実行も予測・評価値が一致。
- 先頭ゼロ、欠損・無限値・不正オッズ、頭数不足、重複、タイムゾーン不足、同着・未確定結果、曖昧な訂正版、上書き拒否、失敗記録を検証。
- 将来のオッズと結果を変更しても予測が変わらず、結果の変更が評価にのみ反映されることを確認。
- 二値Log Loss/Brierとレース単位指標の違いを手計算可能なケースで確認。
- O1の公式バイト配置を模した人工レコードで、通常値・無投票・取消・上限打切り・最終・削除の扱いを確認。実レコードとの照合ではない。
- 取得要求案CLIはnetwork_requests=0、transport_implemented=falseを表示。JV-Linkへの接続は行っていない。
- 合成デモのログはoutputs/offline-demo-v2に保存。評価値は能力の証明ではなく計算テストの値。

### 実装上の制限と未完了項目

- 実通信アダプター、RA/SE変換とO1との結合、取得時刻・公表時刻の原本照合は未実施。
- O1はヘッダーと単勝部分のみ。データ全体の適合検証・予測入力への自動変換はしない。
- ROIは購入条件・公式払戻が未確定で未算出。Calibrationは集計表まで。
- 人気・オッズ帯・年度別評価は実装済みだが、実データの評価ではない。
- 同着・取消・返還・上限打切り等を通常データに自動変換する方針は採用していない。
- 実データに基づくPhase判定は保留。モデル選択・特徴量採否・評価期間の確定も行っていない。

### 次の作業

JV-Link導入後、公式検証ツールで小規模な接続確認を行い、通信アダプターと実データ変換を実レコードに照らして仕上げる。
導入前の手順はacquisition.md、内部形式はdata-contract.md、再実行はoffline-workflow.md、特徴量定義はfeature-registry.mdに保存。

## 2026-09-21 — JV-Link導入確認と初回接続

- ユーザーがSDK展開・JV-Link導入完了を報告。展開フォルダ名の区切りの差を確認して実在する64bit版SDKを特定した。
- 公式Pythonサンプルを読み、COM生成とAPI呼出し方法を確認。サンプルGUI自体は起動していない。
- 公式JV-Link経由で2026-09-20中山8Rの0B41を1回取得。JVInit/JVRTOpen/JVCloseはいずれも0、178件をEOFまで読み込んだ。
- 既存O1部分解析を実行し178件成功、エラー0件。中間・最終・確定の区分を保持した。
- 変更対象は検証記録と進捗文書。本番の特徴量・評価指標・期間は変更していない。
- 取得内容と時刻・ハッシュはGit除外のdata/raw配下に保存。ローカル接続試験スクリプトはwork配下。外部公開はコード・手順・接続成否の記録のみ。
- 過去データの事後取得のため、そのままリアルタイム予測の証拠にはしない。時点整合性の確認とRA/SE結合は未実施。
- 詳細: connection-check.md。新規有料契約・利用キー設定・追加インストールは行っていない。

## 2026-09-21 — RA/SE対応確認と文字列変換問題の修正

変更対象: scripts/collect-jvlink.ps1、ingestion/jv_race.py、単体テスト。
理由: 取得した単勝オッズを実際の出走馬・確定結果に対応付け、入力破損と後知恵の混入を検出する。
影響範囲: 1レースの収集・部分解析・監査のみ。特徴量、評価指標、学習期間、依存パッケージは変更なし。

- 2026-09-20中山8Rについて0B12・0B15を取得。各18件（RA 1、SE 16、HR 1）。RA/SEは区分6。HRは保存のみ、払戻解析は未実装。
- 初期JVRead試験でSEの`Invalid RA/SE length or terminator`を検出。555文字をCP932へ再符号化すると561～570バイトとなり、555バイトで切る既存試験方式では破損することを確認した。OS設定の変更はせず、JVGetsのmanaged byte[]受取へ変更。破損した試験フォルダは検証用に保持し入力から除外した。
- JVGetsで再取得したRA/SEを厳密な固定長・CRLF検証で解析。16頭の馬番号・血統登録番号が一意で、既存O1 178件の馬番号集合と全件一致。通常確定結果の順位1～16が重複なく揃うことを確認。
- 取得スクリプトは0B12/0B15/0B41、単一レース限定。新規フォルダ必須、読取ループ30秒/2000件上限、EOF・エラー・取得時刻・サイズ・SHA256を保存。同期COM呼出そのものを強制タイムアウトする機能ではない。
- オフライン監査CLIは原本ごとのハッシュを出力。取得manifestとの照合は未実装として明示（capture_manifest_verified=false）。対応一致はデータ完全性・時点証明の認定ではない。
- リーク確認: 過去の0B15も結果版であり、過去の発走前出走表とみなさない。作成日を公開時刻に変換せず、prediction_ready=falseを固定。結果の確定日時も未確認。今回の出力は学習入力や実力評価に使用しない。
- テスト: 64 passed。新規12件は固定長、非対応区分、馬重複、頭数不足、版不一致、レース不一致、オッズ集合不一致、不正順位、異常区分・同着・未確定値を確認。実通信後の監査CLIも成功。

判定: DATAは1レースの対応のみ確認、LEAKAGEは時刻根拠未完了、CODE/TESTは今回範囲PASS、VALIDATION未実施、DOCUMENTATION更新済み。Phaseの完了・昇格なし。

次は収集manifestの検証と、発走前に取得した出走表・オッズを保存する観測経路を整備する。過去データで評価するには公表時点の根拠レビューが必要。現在の接続確認のために追加契約やインストールを求める必要はない。

## 2026-09-21 — 取得manifest検証と発走前観測経路

変更対象: ingestion/capture.py、observed.py、jv_race監査CLI、収集スクリプトの表示、テスト・手順書。
理由: 前回未実装だったmanifestと原本の照合を行い、事後取得や結果情報を混ぜずに実観測から既存市場ベースラインへ接続する。
内容: ファイル集合・サイズ・SHA256・API成否・取得時刻順序・対象レースの検証、区分2の出走表と区分1の中間オッズ限定の変換、時刻締切の強制、既存の正規化処理、失敗状態と入力/出力/コードのハッシュ保存。
影響範囲: 新しい観測用CLI。既存の合成デモ・学習期間・特徴量の式・評価指標は不変。既存監査CLIはmanifest必須となり、旧試験フォルダは受入不可。手順を更新した。

- 2026-09-21中山8Rの出走表17件を04:53 JST頃に取得。manifest検証は成功したが、RA/SEはすべて区分9。オッズ0B41は`JVRTOpen failed: -1`、records=0、complete=false。入力を拒否し、予測なし・FAILED記録のみ。失敗を補完値や過去オッズで回避していない。
- 2026-09-20中山8Rのオッズ178件を新方式で再取得。出走・結果18件のmanifestと照合し16頭の対応監査に成功、capture_manifest_verified=true。ただしprediction_ready=falseを維持。
- リーク確認: RA/SE結果版・中止、最終オッズ、締切後取得、未来の発表時刻を拒否。SEの着順等を予測テーブルへ渡さない。取得時刻は可用時刻の保守的な上限であり公開時刻と同一視しない。固定締切モデルの成績やリアルタイム予測実績とは主張しない。
- 単体・結合検証: 95 tests passed。ハッシュ改変、欠落・追加ファイル、パス逸脱、件数、API成否、時刻順序、区分、異常オッズ、同時刻訂正版、年越し、予測再現性、失敗記録、上書き拒否を確認。合成2頭の市場確率は2/3・1/3で再実行一致。
- 制限: 中間オッズ以外や取消等は未対応。PC時計・配信元時刻の独立検証、変更追跡、自動定期収集は未実装。市場性能の実測、学習・期間選定は未着手。

判定: DATAは原本整合性までPASS・発走前有効入力は未完了。LEAKAGEは今回の拒否テストPASS・本番運用の時刻検証は未完了。CODE/TEST/DOCUMENTATIONは今回範囲PASS、VALIDATION未実施。Phase昇格なし。
次は、有効な発走前出走表と中間オッズが提供されるレースで観測経路を通し、予測を保存してから結果と評価を別工程で結ぶ。詳細はobserved-workflow.md。

## 2026-09-21 — 初めての発走前実データ予測を保存

変更対象: observed.py、jv_race.py、収集スクリプトの表示、回帰テスト。
理由: 阪神1Rの実データで露呈した、RAの設定時期とオッズ履歴選択に関する過剰な受入条件を、公式仕様と観測証拠に基づき修正する。

### 取得と予測

- 中山の区分9は、台風による9月21日の開催中止という公式発表と整合。https://www.jra.go.jp/news/202609/092004.html
- 阪神1R（2026092109040701）について05:08 JST頃にRA2 1件・SE2 8件、O1中間オッズ116件を取得。すべてmanifest検証成功。
- RA作成日9/21・SE作成日9/20の差を検出。一度だけ再取得した結果、全レコードのSHA256集合が一致。両取得とも予測締切前。
- 最新は05:05発表の8頭分の正常オッズ。05:14:43.7948847 JSTを診断用締切とし、05:14:47.675798 JSTに予測保存。RAの予定発走は10:00 JST。結果は未取得・未使用。
- 実験IDはobserved-20260921-hanshin01-v3。ローカルoutputs配下に保存。1レース8頭の確率合計は浮動小数点誤差内で1（0.9999999999999998）。同一入力・締切の別フォルダ再実行でpredictions.csvがバイト一致。
- 保存時predictions.csv SHA256: `854de6e4a2eab2256ebe7ace766e965226bc7636b2e68e3d80638cccc7237097`
- 保存時status.json SHA256: `8a64a3d149887fd994b124e8c1d7b207b27dac617547b0a3c44c5930a010b561`
- 予測内容と原本はGit非公開。上記ハッシュと実行記録のみ保存し、後日の比較対象を固定する。外部時刻認証の代わりではない。

### エラーと修正根拠

1. `Inconsistent RA/SE revisions`: 作成日はレコードごとの日付であり、共有の版番号と同一ではない。日付差を残し、独立した後続取得が全バイト一致し、同一レース・区分・頭数・ID集合を満たす場合だけ受け入れる。未確認・変更・締切後の再取得は拒否。一致は配信元の原子的スナップショット保証ではない。
2. RA項番47（884バイト目）の出走頭数は実際に出走した頭数で、区分2では初期値0。従来は実頭数一致を求めており、人工fixtureも実仕様と違っていた。仕様どおり未設定0を検証し、予測対象数はRA登録頭数・SE集合・O1頭数で照合する。結果の頭数を流用しない。
3. `Missing, cancelled, capped or unquoted odds unsupported`: 発売直後の古い履歴に無投票があり、履歴全件へ正常値を要求していた。時刻で最新を選んでから正常値を検査するよう修正。最新に異常がある場合は停止し、古い正常値へ戻さない。全履歴の原本は保持する。
4. PowerShellの要約表示がnullだったため、OrderedDictionaryをPSCustomObjectへ変換して表示。元のmanifestファイルは正常だった。

仕様根拠: https://jra-van.jp/dlb/sdv/sdk/JV-Data4901.pdf （RA項番47、O1）、https://developer.jra-van.jp/t/topic/1035 （再取得・整合性確認）。
影響範囲: 発走前データ受入れと診断のみ。モデル・特徴量の式・評価指標・学習期間は変更なし。リーク確認は上記の時刻・区分・再取得条件と結果未使用で実施。

検証: 103 tests passed。追加8件で日付差の確認なし/一致/変化/時刻違反、RAの結果頭数の混入、過去の無投票、最新無投票時に後退しないことを確認。実予測の4出力ファイルのハッシュと再現性も確認済み。

判定: DATAは1レースの発走前観測成功。CODE/TEST/DOCUMENTATIONは今回範囲PASS。LEAKAGEは入力検査を通過したが外部時計検証・運用全体の監査は未完了。VALIDATIONは結果待ち。Phase 1完了・学習への移行はまだ認定しない。
次は10:00予定のレース終了後に0B12を別フォルダへ取得し、保存済み予測を変更せずに結果の対応を確認する。結果確定時刻と取得時刻を区別した評価経路の整備も必要。自動再開・定期取得は設定していない。

## 2026-09-21 — 保存済み予測の結果評価経路を整備

変更対象: score_saved.py、evaluation/baseline.pyの観測結果用入口、tests/test_score_saved.py、手順書。
理由: レース終了を待たずに準備できる結果照合・評価処理を完成させ、予測の上書きや結果確定時刻の推測を防ぐ。
内容: 事前記録済みstatus SHA256を指定し全出力を検証してから結果を読み込む。区分6/7の0B12から正常な全馬順位のみを受け入れ、既存v1の評価式を共通関数で実行。結果にはresult_observed_atを保存し、正確なsettled_atは生成しない。評価結果・コード・原本のハッシュを記録し、新規出力フォルダだけへ保存する。
影響範囲: 評価用CLIと共通の指標計算。既存evaluateの入力契約・評価式、予測モデル・特徴量・期間は変更なし。観測結果用evaluate_observed_resultsを追加。ROIは引き続き未算出。

- テスト125件成功（追加22件）。手計算可能な2頭のLL/Brier、元予測の不変性、5種の保存ファイルの改変、事後予測、結果未確定、同着、異常区分、順位重複、ID・馬番号・頭数・開始時刻変更、作成日不一致、発走前結果取得、取得種別間違い、欠損を検証。
- 合成の勝者だけを変更すると評価値だけが変わり、保存予測はバイト単位で不変。これらはテスト用レコードであり実成績ではない。
- 実際の阪神1R保存予測（8頭）を記録済みstatus SHA256で検証し成功。結果ファイルにはアクセスしていない。実予測・生データは変更していない。
- 作業開始時刻05:19 JST、予定発走10:00 JSTのため実結果はまだ評価できない。結果取得の空振りを繰り返さず、終了後の一回取得手順をsaved-evaluation.mdに保存。予約実行は設定していない。
- リーク確認: 評価コードに予測再計算処理なし。結果読み込み前に事前予測の生成時刻・原本ハッシュを検証。結果の取得時刻は確定時刻と別名で記録。取消等は無断で行削除・確率再正規化せず停止する。

判定: CODE/TEST/DOCUMENTATIONは今回範囲PASS。DATAは発走前予測保存済み・対象実結果待ち。LEAKAGEはテストPASS・独立時計検証などの運用監査は未完了。VALIDATION未実施、Phase完了や学習移行なし。
次は阪神1R終了後、手順に従って0B12を取得し、保存予測への初回実結果評価を行う。その後、同一手順で対象レースを増やす前に予測締切・評価期間などの実験条件を整理する。

## 2026-09-21 — 初回実結果評価の完了

前回利用制限で自動承認レビューが実行できず未送信だったコミット156b40cを、利用制限解除後にGitHubへpushした。

- 阪神1R（2026092109040701）の0B12を16:05 JSTに取得、10件をEOFまで正常読取。RA・8頭のSEを照合、HRは保存のみ。
- 固定済みstatus SHA256を指定し、早朝保存の8頭分の予測を再計算せず評価。結果はPASS。
- 1頭あたり二値Log Loss 0.4340119717467623、二値Brier 0.1358176455115752。レース単位の勝者Log Loss 2.3844785194656426、多クラスBrier 1.0865411640926015。ROIは未算出。
- 予測4ファイル・評価出力のハッシュを再確認。標準ライブラリでLL/Brierを別計算して一致。JRA公式結果の勝者馬番号とも一致。
- 変更対象は結果レポートと進捗。コードの変更なし。前回の125 tests passedを引き継ぎ、今回は実通信・実データの結合評価と独立計算を追加確認した。
- リーク確認: 事前に記録した予測のハッシュが維持されている。結果による再予測・再正規化・特徴量変更なし。16:05は取得時刻であり確定時刻と同一視していない。
- 原本・個別予測はローカルdata/outputsのみ。集約指標・検証方法・ハッシュをfirst-live-evaluation.mdに保存。

判定: DATA/CODE/TEST/DOCUMENTATIONは今回の1レース検証範囲でPASS。LEAKAGEの入力検査は成功、独立時計検証等の運用全体の監査は未完了。VALIDATIONは1レースの動作確認のみで、未知期間の性能確認は未完了。Phase昇格なし。
次は正式な予測締切、評価期間、対象レース・例外条件を整理する。今回の成績に合わせて選択せず、条件を確定してから継続観測・複数レース評価へ進める。

## 2026-09-21 — 複数レース試験の条件案と保存評価の集計

変更対象: aggregate_saved.py、集計テスト、未採用の実験条件JSONと計画書。
理由: 個別レースの動作確認から複数レース検証へ進む前に、対象・締切・例外を固定できる具体案を用意し、保存済みの評価を正しくまとめる。
内容: 明示台帳からstatus/全ファイルのハッシュ検証、予測とラベルから各評価値の再現、重複拒否、全行を結合したv1集計、条件の未検証箇所を出力。条件案は発走10分前、最大オッズ経過5分、9/26・9/27・10/3・10/4の平地競走。未採用・自動実行なし。
影響範囲: 記述的集計と計画案のみ。特徴量の式・評価指標・既存予測・学習期間は変更なし。Train/Validation/Testは未設定。固定締切の強制と全対象台帳の運用はまだ実装していない。

- 全体テスト130件成功後、2レース結合テストを追加して集計テスト6件を確認（既存全体の残りは不変）。
- 実際の阪神1Rをハッシュ付き台帳で集計し、元のmetrics.jsonと全集計表が同じハッシュになることを確認。予測の書換えなし。
- リーク・選択確認: 同一レースの二重加算、改変・欠落した結果の無視を禁止。指定された台帳以外のレースは探索しない。完全な母集団網羅性は保証しないと明示。早朝の実験は固定10分前試験に混ぜない。
- 条件採用は保留。AGENTS.mdの特徴量定義・Train/Test期間を無断で変えないルールに照らし、利用時点を変える締切の正式採用についてユーザーに確認する。今回のJSONは実行不能な案として保存。

判定: CODE/TEST/DOCUMENTATIONは今回範囲PASS。複数レースの実観測DATA/VALIDATIONは未実施。Phase昇格なし。詳細はmulti-race-pilot-plan.md。

## 2026-09-21 — 承認済み条件の実装と日別台帳

ユーザーが「その条件で準備を進めていい」と明示承認。9/26・9/27・10/3・10/4のJRA平地、発走10分前締切、オッズ経過5分以内、保存締切後1分以内をmarket-observed-pilot-v1.jsonへ正式採用した。旧draftは履歴として保持し、実行用loaderは拒否する。

変更対象: pilot_policy.py、pilot.py、observedの任意policy接続、RAのトラックコード部分解析、score_savedのテスト用内部時計関数、台帳・時刻のテスト、設定・台帳説明・特徴量台帳。
理由: 承認された検証条件を実行時に強制し、成功レースだけの集計を避ける。
内容: 指定日/平地/固定締切/オッズ経過/保存時刻検査、全場確認済み日別台帳の事前固定、SQLiteによる重複実行拒否と状態履歴、1レース単位のJV-Link収集への接続、結果取得後の既存評価、対象全件の状態・理由別集計。
影響範囲: 新パイロットの運用経路。任意時刻の旧診断は維持し新パイロットへ混ぜない。モデルの式・評価指標・Train/Validation/Test期間は変更なし。トラックは対象判定だけに使いML特徴量に投入しない。

- 全体テスト155件成功後、保存中の締切超過・発走時刻変更・収集先から予測への自動引継ぎの3件を追加。試験関連27件すべて成功（従来131件と合わせて158件の構成）。合成データで台帳→収集代替→予測→結果→指標・網羅状況まで検証。実JV-Link収集の新規実行ではない。
- 5分の境界、締切後60秒の境界、対象日外、障害、台帳欠落・重複・場の不一致、承認設定/台帳改変、再予測拒否、取得失敗、締切超過、結果待ちを検証。
- 収集は締切前3分間に0B15/確認0B15/0B41を順次取得する即時コマンド。予測は締切後に別コマンドで実行。各収集子プロセスは60秒上限、締切超過は使用しない。定期起動や常駐プロセスは未設定。
- リーク確認: 予定一覧は締切前に固定。結果による予測再計算なし。取得・保存時刻が条件外ならFAILED/欠測を残す。失敗/障害/未処理を全対象レポートから消さない。頭数・時刻変更を後から自動採用しない。
- 網羅性の限界: 申告した場の全レース番号を検査するが、開催場の申告漏れは元の公式資料の確認が必要。外部時刻認証、電源断後の自動復旧、自動スリープ制御は未実装。
- 本番対象4日分の正式出馬表はまだ確認しておらず本番台帳は未作成。今は承認済み設定と実行経路の準備を完了。次に出馬表公開後の台帳確定と起動方法の整備を行う。

判定: CODE/TEST/DOCUMENTATIONは今回準備範囲PASS。正式条件の実観測DATA/VALIDATIONは未実施、独立時計監査も未完了。Phase昇格なし。操作手順はpilot-operations.md。

## 2026-09-21 — 観測日の常駐起動と結果一括取得

変更対象: pilot_runner.py、pilot_finish.py、運用手順、専用テスト。
理由: 承認された締切を人のコマンド入力時刻に依存せず実行し、全対象の結果確認を一括処理するため。
内容: 実行予定の確認、取得枠の競合拒否、発走13分前取得・10分前予測の常駐処理、SQLite専用ロック、追記ログ、再起動時の二重予測防止、予測終了後の結果一括取得と網羅レポート。
影響範囲: 運用経路の追加のみ。予測式・特徴量・評価指標・期間は不変。OSの予約起動は設定していない。実際の未来日の台帳も未作成。

- 全体166件成功後、競合枠拒否1件と結果一括処理5件を追加。追加テストは個別に全件成功。時刻制御、二重ロックと解放、取得失敗、途中再起動、保存済み予測の再実行回避、早すぎる結果取得拒否、結果再取得時の新規保存先を確認。
- リーク確認: 常駐経路も既存の観測時刻・締切・ハッシュ検証を通る。結果取得を全予定発走後に分離し、既存の保存済み予測のみ評価する。失敗を母集団から除かない。
- Windows電源設定を読み取り、AC/DC双方の自動スリープが0（なし）であることを確認。設定は変更していない。電源断・ネット切断・手動スリープを防ぐ保証ではない。
- 残作業: 正式出馬表の公開後に全場台帳を作成・固定、当日のPC稼働確認、実データ観測。外部時計認証とOS時計変更検出は未実装。

判定: 今回のCODE/TEST/DOCUMENTATION範囲PASS。予定4日分のDATA/VALIDATIONは未実施。Phase昇格なし。

追記: 最新の全体テスト172件すべて成功。Windows時刻同期の読み取り確認は最初の権限確認時に利用制限で実行されなかったが、制限解除後に実行できた。w32tmはLeap Indicator=3 / Stratum=0（未同期）を返した。OS時計は変更していない。発走10分前の観測開始前に時刻同期の復旧確認が必要。生データや既存予測の時刻を書き換えない。

## 2026-09-21 — 時計同期復旧と定期確認の設定

ユーザーがWindowsの「今すぐ同期」を実行し、予定4日間の9〜17時ごろのPC稼働を維持できる見込みと回答。再確認でLeap Indicator=0、Stratum=5、最終正常同期2026-09-21 23:24:10、time.windows.comを確認した。Windows Timeの正常状態を確認したもので、保存済みデータの外部タイムスタンプ認証ではない。

ユーザーの継続作業依頼に基づき、このタスクに朝9時・夕方17時の定期確認を作成した（アプリautomation ID: jra）。公式出馬表公開後の全場台帳固定、開催朝の起動確認、終了後の結果評価を継続する。状態不変では通知せず、進展・失敗・必要操作を通知する。観測終了・結果集計後に停止する指示を含めた。

本番台帳・実観測プロセスはまだ作成・起動していない。定期確認は秒単位の実行保証ではなく、予測締切はローカルrunnerが制御する。ローカル定期確認にはPCとCodexアプリの稼働が必要。今回は運用記録のみ変更し、直前の172件成功からコード変更なし。Phase昇格なし。

## 2026-09-21 — 開催待ちと分離して過去データ取得を開始

直前ターンは方針説明のみで進捗なしだったため、実データ取得へ移行した。
変更対象: scripts/probe-jvopen.ps1、取得説明。目的: 既存の速報系だけでなく蓄積系RACEへのアクセスと少量保存を確認する。

- 過去の2026092006040608の確定結果0B12を18レコード取得し、既存O1時系列178件との16頭の対応・完全順位・manifestを検証した。
- SDK同梱JV-Link仕様書17〜19ページの期間指定を確認。JVOpen/RACE/option=1は指定期間に提供された情報であり、開催日の範囲ではない。
- 最大1日、20ファイル/申告10MiB、読取90秒/5000レコードの診断・小規模保存スクリプトを追加。初回セットアップや利用キー設定は行わない。
- 実接続はJVInit/JVOpen/JVCloseすべて0。3ファイル230KB、688レコード（JG344、RA24、SE320）を保存し、全サイズ・ハッシュ・終端・ファイル集合を別スクリプトで検証した。
- 提供日9/20の内容は9/21開催24レースの区分2出走表だった。確定結果・学習データ完成と誤認しない。JGは未解析。元データと一覧JSONはローカルのみ保存。
- 次の実装: 取得manifestの汎用検証、レースID・データ区分・改訂別の蓄積、結果レコードの取得範囲確認、当時利用可能な出走表と履歴の対応。学習/Validation/Test期間は実データ網羅性の確認後に提案し、未承認の期間は採用しない。

影響: 小規模取得経路のみ。既存予測・モデル・指標は不変。リーク確認: 過去の取得時刻を当時の公表時刻へ置換せず、training_ready=falseを保持。実通信と全原本の完全性確認を実施。Phase昇格なし。

## 2026-09-21 — 蓄積データの検証・改訂索引

変更対象: ingestion/historical.py、tests/test_historical.py、取得手順。
理由: 取得バイト列から、学習データ作成で使うレース・データ区分・改訂の追跡へ進むため。
内容: 完了manifest・COMコード・サイズ/SHA256・ファイル集合・時刻順序を検証し、RA/SEの部分解析と全改訂の索引を保存。未対応種別や区分も理由付きで保持。結果の最新値による出走表の上書きをしない。

- 専用テスト10件成功。改変・欠落・余分なファイル・パストラバーサル・未完了・時刻異常・boolの終了コード・未対応改訂・同一レース複数改訂の保持を確認。
- 実取得688レコードの索引化でRA/SE344レコード・24レースを解析。JG344件も原本参照付きで保存。
- 提供期間9/21 00:00〜22:00を少量追加取得。JVInit/Open/Closeすべて0、3ファイル116KB、351件（JG178、RA12、SE161）を検証・索引化。開催日は9/22、RA/SEは区分2であり、結果取得には至っていない。
- この標本だけで蓄積データに結果がないとは判断しない。次は公式仕様の提供スケジュールと取得方式を確認し、成績履歴を加える。

影響範囲: 取込の前段のみ。特徴量・モデル・指標・期間は変更なし。リーク対策: availability_at=null、training_ready=false、latest_revision_selected=falseを維持し、取得日時を当時の利用可能時刻に読み替えない。今回のCODE/TEST/DOCUMENTATION範囲PASS、学習用DATA/VALIDATION未完了、Phase昇格なし。

## 2026-09-21 — 過去の成績48レースを取得

公式JV-Data仕様書のデータ提供タイミングを確認し、前週月曜9/14の提供分を要求した。最初は23ファイル・11563KBで診断用20ファイル/10MiB上限に達したため、読み出さずに正常終了し失敗理由を記録。判明した小規模データを扱うため上限を30ファイル/16MiB、20000レコードへ変更し、新規保存先で取得した（90秒読取上限は維持）。

- 23ファイル、1112レコードをEOFまで取得。JVInit/Open/Closeすべて0。全原本のハッシュ・サイズ・終端・ファイル集合・時刻を検証。
- 成績区分7のRA48・SE630が含まれ、対象開催日は9/12・9/13。初めて蓄積系でまとまった成績履歴を取得できた。
- その他H1/H6/HR/O1〜O6/WFも保存したが、予測特徴量に自動投入しない。O1は区分5の確定情報であり、発走10分前オッズには使わない。
- 48レースすべてのRA/SE/O1の識別と頭数の対応は検査成功。通常の完全順位条件に適合した平地は40レース。残る8件は異常区分や同着等の個別確認が必要であり、母集団から消さず品質レポートに残す。
- 取込テスト10件成功。モデルや評価指標、学習期間を変更せず、training_ready=falseを保持。

変更対象: 小規模取得上限と対応する取込上限、取得説明。理由: 実測11.3MiBの1提供日分を取得するため。影響: 取得検証のみ。学習データ完成やPhase完了は未宣言。次は蓄積の拡張と過去走の時点結合を進める。

## 2026-09-21 — 最初の学習条件をユーザー承認

ユーザーが期間・9特徴量・ロジスティック回帰の提案を明示承認した。configs/logistic-initial-v1.jsonに固定。Train=2025-10-01〜2026-06-30、Validation=2026-07-01〜08-31、Test=2026-09-01〜09-13（日本時間の開催日、レース単位）。特徴量は年齢、性別、枠番、馬番、頭数、距離、馬場種別、競馬場、負担重量。

9/12・9/13の結果データは承認前に原本・完全順位・異常コード等の品質確認に利用しているため、その閲覧履歴を設定に明記した。モデルの学習・調整・性能選択には未使用。今後Testはモデル選択に使わない。取得範囲と各特徴量の当時利用可能性を確認してから実行する。準備の承認であり、未検証のデータを学習可能と宣言したものではない。

## 2026-09-22 — 初期モデルの原数値解析

変更対象: ingestion/model_fields.py、専用テスト、特徴量台帳。承認済み9特徴量のうち、RA/SEから直接得る値を公式バイト位置で部分解析する処理を追加した。頭数は結果用の実出走頭数を使わず、既存の締切時点出走表の照合経路で決める。

- SDK同梱JV-DataのRA/SEの位置とコード2009を照合。芝10〜22、ダート23〜26/29、サンド27/28を区別。最初の実装でサンドをダートへ含めていた点を実適用前に修正した。
- テスト9件成功。負担重量0.1kg→kg、欠損保持、芝/ダート/サンド境界、障害対象外、不正文字の拒否を確認。
- 実出走表24レース320頭から解析成功。馬ごとの5項目は欠損0/320、距離・競馬場・登録頭数は欠損0/24。surfaceは平地分類対象外を含めnullが1/24。原本ハッシュ付き点検JSONをローカル保存。
- availability_at=null、training_ready=falseを維持。後日取得した成績値を当時の入力として認定しない。モデルの学習・検証はまだ実行していない。

影響: 承認済み入力の原値解析のみ。結果や確定オッズは原値辞書に含めず、期間・指標は不変。特徴量台帳の全項目を追記。次は原本の供給範囲と時点を確認して学習用結合と前処理へ進む。Phase昇格なし。
